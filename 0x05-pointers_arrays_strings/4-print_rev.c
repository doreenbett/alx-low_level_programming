#include "main.h"
/**
* print_rev -prints a string in reverse
*
*@s: the character that points the string
*/
void print_rev(char *s)
{
	int l >= 0

	while (s + l)
	{
	l--
	}
	putchar(s);
	putchar('\n');
}
