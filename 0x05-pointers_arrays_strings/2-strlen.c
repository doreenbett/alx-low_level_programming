#include "main.h"
/**
* _strlen - returns the length of a string
* @s: is te ointer to the length of string
* Return: _strlen
*/
int _strlen(char *s)
{
	int len;

	*s = len;
	putchar(len);
	putchar('\n');
	return (_strlen);
}
