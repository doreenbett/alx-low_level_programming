#include "main.h"
/**
* _strlen - returns the length of a string
* @s: is te ointer to the length of string
* Return: strlen
*/
int _strlen(char *s)
{
	int len;

	*s = len;
	_putchar(len);
	_putchar('\n');
	return (strlen);
}
