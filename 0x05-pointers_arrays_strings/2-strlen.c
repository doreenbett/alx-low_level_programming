#include "main.h"
/**
* _strlen - returns the length of a string
* @s: is the pointer to the length of string
* Return: 0
*/
int _strlen(char *s)
{
	int length = 0;

	while
		(*s++);
		length++;

	return (length);
}
