#include "main.h"
/**
*more_numbers - Prints 10 times the numbers
*
*/
void more_numbers(void)
{
	int num
	int i

	for (i = 0; i <= 10; i++)

	for (num = 0; num <= 14; num++)
	_putchar(num);
	_putchar('\n');
}
